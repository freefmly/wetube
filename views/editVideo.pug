extends layouts/main

block content
    .form-container
        form(action=`/videos${routes.editVideo}`, method="post")
            input(type="text", placeholder="동영상 제목", name="title")
            textarea(name="description", placeholder="설명")
            input(type="submit", value="업데이트")
        a.form-container__link.form-container__link--delete(href=`/videos${routes.deleteVideo}`) 동영상 삭제